/*  
math -> biblioteca nativa do JS que possui diversas funcionalidades matemáticas
random() -> função que gera um número aleatório entre 0 e 1
floor() -> função que arredonda um número para baixo
ceil() -> função que arredonda um número para cima
round() -> função que arredonda um número para o inteiro mais próximo
pow() -> função que eleva um número a uma potência
sqrt() -> função que calcula a raiz quadrada de um número
abs() -> função que retorna o valor absoluto de um número
max() -> função que retorna o maior valor entre os argumentos fornecidos
min() -> função que retorna o menor valor entre os argumentos fornecidos

const result = Math.random() // Gera um número aleatório entre 0 e 1
console.log(result)

const result1 = Math.floor(4.7) // Arredonda para baixo
console.log(result1)

const result2 = Math.ceil(4.2) // Arredonda para cima
console.log(result2)

const result3 = Math.round(4.5) // Arredonda para o inteiro mais próximo
console.log(result3)

const result4 = Math.pow(2, 3) // Eleva 2 à potência de 3
console.log(result4)

const result5 = Math.sqrt(16) // Calcula a raiz quadrada de 16
console.log(result5)



// OPERADORES DE COMPARAÇÃO
// > maior que
// < menor que
// >= maior ou igual a
// <= menor ou igual a
// == igual a (compara apenas o valor)
// === estritamente igual a (compara valor e tipo)
// != diferente de (compara apenas o valor)
// !== estritamente diferente de (compara valor e tipo)

const result6 = 5 > 3 // true
console.log(result6)

const fistnumber = 5
const secondnumber = 10
if (fistnumber < secondnumber) {
    console.log(`${fistnumber} é menor que ${secondnumber}`)
} else {
    console.log(`${fistnumber} não é menor que ${secondnumber}`)
}
*/